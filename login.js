const form=document.querySelector('#loginForm'),error=document.querySelector('#loginError');if((()=>{try{return JSON.parse(localStorage.getItem('trueReadAuth')||'null')?.username==='ximiyang'}catch{return false}})())location.replace('index.html');form.onsubmit=e=>{e.preventDefault();const username=document.querySelector('#loginUser').value.trim(),password=document.querySelector('#loginPassword').value;if(username!=='ximiyang'||password!=='ximiyang'){error.textContent='账号或密码错误，请重新输入。';return}localStorage.setItem('trueReadAuth',JSON.stringify({username:'ximiyang',signedAt:Date.now()}));const raw=new URLSearchParams(location.search).get('next')||'index.html',next=/^(index|todo)\.html(?:#[\w-]*)?$/.test(raw)?raw:'index.html';location.replace(next)};
